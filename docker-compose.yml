# edit the app and it watches it when you change files
services:
  web:
    tty: true
    stdin_open: true
    build: .
    ports:
      - "4000:4000"
    # command: bundle exec foreman start
    command: bundle exec foreman start
    develop:
      watch:
        - action: sync
          path: .
          target: /usr/app